replicaCount: 2

image:
  repository: devops-demo
  tag: "1.0.0"
  pullPolicy: IfNotPresent

env:
  SPRING_PROFILES_ACTIVE: "dev"

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    memory: 768Mi
